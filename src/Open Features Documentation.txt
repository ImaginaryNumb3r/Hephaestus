Open Features:
    Contracts
        - Beginning: Null and other common checks for parameters
            -> e.g. Contract.NotNull(Object...);
        - Returning: An AutoClosable ResultContract that is called before the end
            -> eg:
    Own Streams
        - Better sources
            -> Stream.of <- equivalent that also allows arrays/Collections/Iterables
        - Better sinks
            -> to Collectors.toArrayList/toLinkedList